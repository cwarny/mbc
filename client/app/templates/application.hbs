<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<a class="navbar-brand" href>MBC</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="collapse navbar-collapse">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item">
				{{link-to 'Orders' 'orders' tagName='a' class='nav-link'}}
			</li>
		</ul>
		{{#if session.isAuthenticated}}
			<button class="btn btn-outline-danger pull-right clickable" onclick={{action 'logout'}}>Logout</button>
		{{else}}
			<button class="btn btn-outline-success pull-right clickable" onclick={{action 'login'}}>Login</button>
		{{/if}}
	</div>
</nav>
<div class="main container-fluid">
	<div class="row justify-content-center">
		<div class="col-10">
			{{outlet}}
		</div>
	</div>
</div>